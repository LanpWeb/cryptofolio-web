// @flow

export type Props = {
  caption?: string,
  textBtn?: string,
  className?: string,
  active?: boolean,
  edit?: boolean,
  closeModalHandler?: () => void,
  submitModalHandler?: () => void,
}

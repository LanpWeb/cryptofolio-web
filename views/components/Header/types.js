// @flow

export type Props = {
  router: any,
  items?: Array<{ title: string, route: string }>,
  auth: boolean,
  email: null | string,
  router: any,
  items?: Array<{ title: string, route: string }>,
  signOut: () => void,
}
